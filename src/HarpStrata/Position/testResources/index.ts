import { PositionIds } from '../types'

const FIRST_POSITION: {
  id: PositionIds.First;
  root: 0;
} = {
  id: PositionIds.First,
  root: 0,
}

export const EXAMPLE_POSITIONS: {
  FIRST_POSITION;
} = {
  FIRST_POSITION,
}
