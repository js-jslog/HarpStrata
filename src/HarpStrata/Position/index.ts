import { PositionIds } from './types'

export const FIRST: {
  id: PositionIds.First;
  root: 0;
} = {
  id: PositionIds.First,
  root: 0,
}

export const SECOND: {
  id: PositionIds.Second;
  root: 7;
} = {
  id: PositionIds.Second,
  root: 7,
}
